const WRM = {
  APP_NAME: 'Workshop Registration Manager',
  VERSION: 2,
  CONFIG_KEY: 'WRM_CONFIG_V2',
  SUBMIT_HANDLER: 'onFormSubmit'
};

const WRM_DEFAULTS = {
  CLOSED_MESSAGE: 'All workshop sessions are full. Registration is now closed.',
  EVENT_LOOKAHEAD_DAYS: 180,
  MAX_CAPACITY: 10000
};